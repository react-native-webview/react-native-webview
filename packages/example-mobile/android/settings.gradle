pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenLocal()
        google()
    }
}

rootProject.name = "WebviewExample"

apply(from: new File(["node", "--print", "require.resolve('react-native-test-app/package.json')"].execute(null, rootDir.parentFile).text.trim()).getParentFile().toString() + "/test-app.gradle")
applyTestAppSettings(settings)
