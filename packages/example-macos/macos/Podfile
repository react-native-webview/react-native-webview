require_relative(
  `node --print "require.resolve('react-native-test-app/macos/test_app.rb', {paths: ['#{__dir__}/..']})"`
    .strip
)

workspace 'WebviewExample.xcworkspace'

use_test_app! :hermes_enabled => false
